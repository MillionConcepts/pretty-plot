"""user-facing noninteractive pretty_plot utility"""

if __name__ == '__main__':
    print(
        "pretty-plot was restructured as a Python package. "
        "Please install it into your environment with "
        "'pip install .' and then run it with the 'pplot' command."
    )